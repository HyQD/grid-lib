# API Overview

API pages are generated from Python docstrings using mkdocstrings.

If you improve a docstring in the codebase, the rendered API reference updates automatically on the next docs build.
